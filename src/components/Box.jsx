import styled from 'styled-components';
import {
  color,
  space,
  layout,
  flexbox,
  background,
  boxShadow,
} from 'styled-system';

export const Box = styled('div')(
  color,
  layout,
  space,
  flexbox,
  background,
  boxShadow
);
